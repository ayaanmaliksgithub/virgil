# Local development stack.
# The worker mounts the host Docker socket so it can launch per-scan sandbox
# containers. Treat this socket as a privileged interface — production should
# use a dedicated sandbox host or a rootless runtime.

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: audit
      POSTGRES_PASSWORD: audit
      POSTGRES_DB: audit
    ports: ["5432:5432"]
    volumes: ["pg-data:/var/lib/postgresql/data"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U audit"]
      interval: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    ports: ["6379:6379"]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      retries: 10

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minio
      MINIO_ROOT_PASSWORD: miniominio
    ports: ["9000:9000", "9001:9001"]
    volumes: ["minio-data:/data"]

  api:
    build:
      context: .
      dockerfile: docker/api.Dockerfile
    env_file: [.env]
    environment:
      # First-launch UX: the api container's entrypoint runs alembic and
      # seeds an OWASP NodeGoat demo audit on a fresh DB, so localhost:3000
      # is populated immediately on a clean `docker compose up`. Set to
      # "false" to skip the seed; the seed itself is idempotent regardless.
      SEED_DEMO_AUDIT: "true"
    depends_on:
      postgres: { condition: service_healthy }
      redis: { condition: service_healthy }
    ports: ["8000:8000"]
    volumes:
      - audit-uploads:/var/audit/uploads

  scanner-image-builder:
    # Builds the per-scan sandbox image (Semgrep + Trivy + Gitleaks pre-baked).
    # Runs once and exits. The worker waits on this via
    # `condition: service_completed_successfully`, so a fresh
    # `docker compose up` does the right thing in one command — no manual
    # `docker compose up scanner-image-builder` first.
    image: docker:26-cli
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./docker/scanner.Dockerfile:/scanner.Dockerfile:ro
      - ./:/ctx:ro
    command: ["docker", "build", "-t", "virgil/scanner:latest", "-f", "/scanner.Dockerfile", "/ctx"]

  worker:
    build:
      context: .
      dockerfile: docker/worker.Dockerfile
    env_file: [.env]
    depends_on:
      postgres: { condition: service_healthy }
      redis: { condition: service_healthy }
      scanner-image-builder: { condition: service_completed_successfully }
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - audit-work:/var/audit/work
      - audit-uploads:/var/audit/uploads
    environment:
      WORK_ROOT: /var/audit/work

  web:
    build:
      context: .
      dockerfile: docker/web.Dockerfile
    environment:
      NEXT_PUBLIC_API_BASE: "http://localhost:8000"
    ports: ["3000:3000"]
    depends_on: [api]

volumes:
  pg-data:
  minio-data:
  audit-work:
  audit-uploads:
